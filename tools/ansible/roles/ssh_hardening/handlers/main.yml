---
# SSH Hardening Role - Service Handlers
# Triggered when configuration changes require service restarts

- name: restart sshd
  ansible.builtin.systemd:
    name: ssh
    state: restarted
  listen: restart sshd

- name: restart fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted
